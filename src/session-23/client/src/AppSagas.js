import logo from './logo.svg';
import './App.css';
import LoginFormContainer from './components/container/LoginFormContainer';

function AppSagas() {
  return (
    <div className="AppSagas" style={{background: '#333'}}>
      <LoginFormContainer/>
    </div>
  );
}

export default AppSagas;
